# Copie este arquivo para .env e preencha a senha do banco
POSTGRES_PASSWORD=
# Exemplo: POSTGRES_PASSWORD=minha_senha_secreta
# Não use aspas, apenas a senha
# O arquivo .env deve ser mantido em segredo e não deve ser adicionado ao controle de versão (git)